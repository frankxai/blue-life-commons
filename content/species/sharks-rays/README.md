# Species Guild: sharks-rays

Sharks, rays, and skates.

Species intelligence pages live here as `<species-slug>.md` with `type: species-page` frontmatter (see [schema/artifact-schema.md](../../../schema/artifact-schema.md)). Every factual claim needs a source ([SOURCES.md](../../../SOURCES.md)); conservation status must cite a recognized authority such as the IUCN Red List. Science-sensitive pages require expert review.
